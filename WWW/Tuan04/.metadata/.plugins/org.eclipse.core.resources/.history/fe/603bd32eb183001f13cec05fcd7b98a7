package dao;

import jakarta.persistence.EntityManager;

public interface DanhMucDAO {
	private EntityManager entityManager;

    public DanhMucDAO(EntityManager entityManager) {
        this.entityManager = entityManager;
    }
}
